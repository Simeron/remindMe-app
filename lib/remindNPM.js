const rest = require('restler');
const Push = require("push.js");

class PushReminder {

    static doFetchInfo(push) {
        console.log("hello");
  define(['pushjs'], function (Push) {
   Push.create('Hello World!');
});
    }

}

module.exports = PushReminder;

